import styled from 'styled-components';
import { linksBgColor } from '../../utils';

export const SidebarLinksContainer = styled.div`
	background-color: ${linksBgColor};
	border-radius: 2rem;
	height: 100%;
	width: 100%;
`;
