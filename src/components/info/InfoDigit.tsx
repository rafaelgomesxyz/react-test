import styled from 'styled-components';
import { badgeBgColor } from '../../utils';

export const InfoDigit = styled.div`
	background-color: ${badgeBgColor};
	border-radius: 1rem;
	font-size: 1.3rem;
	padding: 0.8rem 1rem;
`;
